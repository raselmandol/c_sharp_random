//Installed RAM: It calculates the total installed RAM capacity by querying the Win32_PhysicalMemory class.
//RAM Usage: It uses performance counters to measure the RAM usage and free RAM.
//Hard Disk Storages: It retrieves information about the available drives, including their names, total size, used space, and free space.
//Processor Info: It uses the Win32_Processor class to retrieve the processor name and load percentage. Additionally,
